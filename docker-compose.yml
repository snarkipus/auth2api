services:
  auth2api:
    build: .
    volumes:
      - ./config.yaml:/config/config.yaml:ro
      - auth-data:/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - default
      - docker-lab

volumes:
  auth-data:
    external: true
    name: auth2api-data

networks:
  docker-lab:
    external: true
