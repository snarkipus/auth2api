services:
  auth2api:
    build: .
    volumes:
      - ./config.yaml:/config/config.yaml
      - auth-data:/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - default
      - docker-lab

volumes:
  auth-data:

networks:
  docker-lab:
    external: true
