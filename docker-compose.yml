services:
  auth2api:
    build: .
    ports:
      - "127.0.0.1:8317:8317"
    volumes:
      - ./config.yaml:/config/config.yaml
      - auth-data:/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  auth-data:
